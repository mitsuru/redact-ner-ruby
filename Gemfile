# frozen_string_literal: true

source "https://rubygems.org"

gemspec

# Optional: supplies a prebuilt libonnxruntime so `ORT_DYLIB_PATH` can be set
# from the gem rather than installed system-wide. See examples/.
group :development do
  gem "onnxruntime"
end
